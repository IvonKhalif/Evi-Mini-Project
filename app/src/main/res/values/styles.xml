<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorRedHalf2</item>
        <item name="colorPrimaryDark">@color/colorRed_6D2916</item>
        <item name="colorAccent">@color/blue_2196F3</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/white_FFFFFF</item>
        <item name="android:windowDisablePreview">true</item>
    </style>
</resources>
